import 'package:flutter/material.dart';

class AppColors {
  // Primary / Brand Colors
  static const Color primary = Color(0xFF00B366); // Aura Green
  static const Color primaryLight = Color(0xFFE8F8F0);
  static const Color primaryDark = Color(0xFF008F52);

  // Neutral / Greyscale Colors
  static const Color background = Color(0xFFFFFFFF);
  static const Color surface = Color(0xFFFBFBFB);
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF757575);
  static const Color border = Color(0xFFE0E0E0);
  static const Color borderFocused = Color(0xFF00B366);
  static const Color error = Color(0xFFD32F2F);

  // Accent / Others
  static const Color black = Colors.black;
  static const Color white = Colors.white;
  static const Color shadow = Color(0x0A000000);

  // Gradient AI Color
  static const Color aiGradientStart = Color(0xFF34A853);
  static const Color aiGradientEnd = Color(0xFF4CAF50);
}
